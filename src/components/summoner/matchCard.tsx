import type {BasicMatchType, SummonerType} from "@/external/types";

export default function MatchCard({match, summoner}: {match: BasicMatchType, summoner: SummonerType}) {
  return (
    <>
      hello
    </>
      )
}
